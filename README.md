installation:
`pip install -r requirements.txt`

configuration: 
`./s3.py configure`

to list all buckets: 
`./s3.py list >size<`

\>size< is optional but must be one of the following:
* b
* kb
* mb
* gb